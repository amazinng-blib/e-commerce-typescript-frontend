import React, { memo } from 'react';
import { ToastContainer } from 'react-toastify';

type Props = {};

const ToastScreen = memo((props: Props) => {
  return <ToastContainer />;
});
export default ToastScreen;
